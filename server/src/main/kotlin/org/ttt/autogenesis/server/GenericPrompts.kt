package org.ttt.autogenesis.server

/**
 * Centralizes the generic fallback prompts used when agents fail to produce a response.
 */
object GenericPrompts
{
    const val PLAYER_GENERIC_FALLBACK = "Maintaining defensive posture and consolidating resources due to momentary command silence."
    const val NPC_GENERIC_FALLBACK = "Expand influence, exploit opportunities, and pressure nearby rivals."
}